import React from 'react';
import { Menu, Modal, Icon } from 'semantic-ui-react';
import { PageHeader, Button, Navbar, ButtonGroup, Table, Glyphicon } from 'react-bootstrap';

// const handleOnClick = () => {
//
// }

export default () => {
  return (
    <nav id="nav">
      <h1>BUSHWICK GENERATOR DIGITAL COMMUNE</h1>
      <img src="/profile-icon.png" alt="profile" onClick={this.handleOnClick}/>
    </nav>
  );
};
